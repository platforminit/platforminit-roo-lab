# P-CH04-T01 — OpenAI changed-files re-review

## Verdict

**APPROVE**

## Findings

No blocking correctness, integration, regression, idempotence, workflow-UX, or acceptance-criteria findings remain in the changed scope.

## Resolved finding

1. **Bounded arithmetic input:** resolved. [`bootstrap-argocd.sh`](../../platform/cluster/addons/argocd/bootstrap-argocd.sh:56) now defines the operator timeout with a 300-second default and explicit 30-1800-second bounds. Before root/tool checks, either wait helper, or any cluster call, the bootstrap requires a digit-only value at [`bootstrap-argocd.sh`](../../platform/cluster/addons/argocd/bootstrap-argocd.sh:112), normalizes it explicitly as base 10 at [`bootstrap-argocd.sh`](../../platform/cluster/addons/argocd/bootstrap-argocd.sh:116), and range-checks the normalized value at [`bootstrap-argocd.sh`](../../platform/cluster/addons/argocd/bootstrap-argocd.sh:117). Both [`wait_for_application_synced()`](../../platform/cluster/addons/argocd/bootstrap-argocd.sh:229) and [`wait_for_object()`](../../platform/cluster/addons/argocd/bootstrap-argocd.sh:244) therefore receive only a bounded integer, preventing recursive Bash arithmetic evaluation of inherited payloads and preventing malformed values from failing after Argo CD mutation.
2. **Regression coverage:** resolved. [`ch04-validate-platform-services-gitops-contract.sh`](../../platform/cluster/validate/ch04-validate-platform-services-gitops-contract.sh:690) checks the lower/upper constants, explicit base-10 normalization, digit-only rejection of arithmetic/nonnumeric payloads, and source ordering before the first actual deadline expression and cluster access. Its negative check treats the payload only as a quoted string and does not source or execute the root-run bootstrap.
3. **Operator documentation:** resolved. [`README.md`](../../platform/cluster/addons/argocd/README.md:29) and [`ch04-platform-services-gitops-contract.md`](../ch04-platform-services-gitops-contract.md:41) document the 300-second default, allowed 30-1800-second range, base-10 requirement, and pre-cluster validation boundary.

## Scope and regression assessment

- The follow-up is limited to the requested bootstrap, focused repository validator, and two operator documents. It does not alter legacy sibling scripts, infrastructure, secrets, manifests, or unrelated task behavior.
- Existing AppProject allowlists/destinations, digest-verified local applies, fixed HTTPS origins, version syntax/pins, source reachability, smoke ownership/cleanup, and existing timeout bounds remain enforced by the focused validator.
- The selected 30-1800-second range is operator-appropriate around the existing 300-second default: it prevents ineffective near-zero waits and unbounded operator sessions while retaining a 30-minute budget for a slow initial reconciliation.
- The bootstrap remains idempotent and destructive actions are unchanged and guarded. No secret value or project-routing boundary was introduced.
- The task remains on `batch/platform-ch04-platform-services-gitops-contract`, within the controller allowlist, and entered review in `needs_review` with `platforminit-openai-reviewer` as next actor.

## Acceptance criteria

The prior Traefik ports 80/443, cert-manager Cloudflare DNS-01 and staging issuance evidence, Argo CD self-management/ApplicationSet ownership, GitOps deployment ownership, and per-component validator evidence remain present. The focused repository validator passes all 112 assertions after the timeout fix.

## Independent validation evidence

- Bash syntax: PASS for [`bootstrap-argocd.sh`](../../platform/cluster/addons/argocd/bootstrap-argocd.sh:1) and [`ch04-validate-platform-services-gitops-contract.sh`](../../platform/cluster/validate/ch04-validate-platform-services-gitops-contract.sh:1).
- Focused repository validator: PASS (`PASS=112 FAIL=0`).
- Runtime-negative repository-local probes: arithmetic payload, alphabetic input, 29, 1801, and 999999999 all failed at the validation boundary; the payload marker remained absent.
- Valid decimal boundary/normalization probes: 30, 0300, 300, and 1800 passed timeout validation and stopped at the later non-root preflight gate, with no cluster access.
- Ordering evidence: digit guard line 112, base-10 normalization line 116, range guard line 117, first deadline arithmetic line 230, first `kubectl` line 232.
- `git diff --check`: PASS.
- `git diff --cached --check`: PASS.
- No infrastructure workflow or cluster/cloud mutation was run.
- Environment deviation: review ran under `/workspaces/platforminit-roo-lab` as `codespace`, not the prescribed WSL path/user. Consistent with the prior controller-authorized review, work was restricted to repository-local reads, syntax/static/runtime-input checks, report generation, and the controller verdict; no infrastructure action was attempted.
