# P-CH05-T01 correctness review

- **Task:** P-CH05-T01 — Verify the current Checkmk GitOps runtime and retired-stack boundary
- **Reviewer:** `platforminit-openai-reviewer`
- **Verdict:** APPROVE
- **Reviewed scope:** `platform/observability/README.md`; `platform/observability/validate/ch05-validate-operations-stack.sh`

## Focused checks

- `git diff --check`: **PASS**, exit 0.
- `CH05_REFERENCE_VALIDATE_MODE=static bash platform/observability/validate/ch05-validate-operations-stack.sh`: **PASS**, exit 0; emitted the expected seven `PASS:` lines (five contract checks, two retired-boundary checks), plus the final success line.
- Default/no-override invocation with `BASE_DOMAIN` unset: **PASS regression check**, rejected before cluster access with `FATAL: Missing BASE_DOMAIN`, exit 1.
- Invalid reference mode (`CH05_REFERENCE_VALIDATE_MODE=bogus`): **PASS**, rejected with the documented fatal error, exit 1.
- Child-produced negative-control harness rerun: **PASS**; baseline accepted and all five negative controls rejected with exit 1, `NEGATIVE_CONTROLS_FAILURES=0`.
- Existing evidence reviewed: reference-state log, contract/diff-check log, negative-controls log, and submit log path supplied by the task handoff. The rerun matched the recorded results.
- Scope: `git diff --name-only` showed the two bounded product changes plus controller-owned task-state files. No additional product/source file was modified; the task-state changes are controller state and outside the implementation patch.
- Environment: WSL check passed; branch is `batch/platform-ch05-checkmk-reference-state`; no infrastructure workflow or live-cluster command was run.

## Acceptance criteria

1. **PASS — active runtime and storage ownership resolve to Checkmk Community under Argo CD.** Static validation independently checked the Application name/namespace, `operations` AppProject, `platforminit.io/gitops-mode: owner`, Helm chart source/release name, Checkmk Community image, operations namespace, and PV/PVC/storage-root relationship.
2. **PASS — retired deployment choices are excluded.** Static validation scans the manifests and Argo CD deployment surfaces for retired identifiers, permits only the declared script guard references, checks retired path absence, enforces the expected template directories, requires one Application ownership template, and verifies the stale `argocd/apps` duplicate is unreferenced. The five private-copy negative controls demonstrate fail-closed behavior for retired chart/script paths, documentation disagreement, and stale duplicate activation.
3. **PASS — documentation and focused validation agree.** The README documents the same Application, project, image, storage, trusted header, and static command that the validator checks. The validator also compares the documented Checkmk data path with the values-driven storage root.

## Correctness and integration assessment

The static mode is isolated before the existing runtime gate, does not require `BASE_DOMAIN`, does not invoke `kubectl`, and exits successfully without mutating the repository or cluster. Unset mode remains `runtime`, and the original `BASE_DOMAIN`, `VALIDATION_MODE`, tool, kubeconfig, Argo CD health, resource, frontend, graph, and optional SSO checks remain after the new dispatch. The no-override check confirms the prior early failure/exit behavior.

The checks are repeatable: the static run and negative-control harness use repository reads/private temporary copies, and the second negative-control run produced the same results. The stale Application limitation is explicitly documented and guarded by reference checks rather than silently treated as active ownership. The static mode is not wired into workflows, but the README accurately states that release workflows continue using runtime mode; this is consistent with the bounded task and declared limitation.

## Unresolved risks / limitations

- No live-cluster facts are proven here; Synced/Healthy state and Checkmk pod behavior still require the runtime validator on the target host.
- `argocd/apps/ch05-observability.yaml` remains a stale, retired-description duplicate outside the changed scope; correctness depends on its continued lack of references, which the static validator checks.
- Static mode is repository-only and not a separate workflow gate; a future workflow change could omit it unless explicitly added.
- The patch is uncommitted and has no remote copy; commit/push remain Release Manager responsibilities.

## Consolidated rework findings

None. Approval is contingent only on the declared live-runtime, stale-duplicate, workflow-wiring, and release-handoff limitations above; none blocks this correctness-review stage.
