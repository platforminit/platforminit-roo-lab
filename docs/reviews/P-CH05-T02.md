# P-CH05-T02 — OpenAI correctness review

- **Task:** Define the CH05 stable and rehearsal GitOps source contract
- **Branch:** `batch/platform-ch05-gitops-source-contract`
- **Review scope:** the five controller-reported changed files only
- **Verdict:** APPROVE

## Acceptance criteria

1. **Stable and rehearsal choices are explicit and deterministic — satisfied.**
   [`ch05-register-operations-stack.sh`](platform/observability/scripts/ch05-register-operations-stack.sh:37) declares the two canonical repository URLs and the stable default revision. [`ch05-register-operations-stack.sh`](platform/observability/scripts/ch05-register-operations-stack.sh:57) resolves an explicit `CH05_GITOPS_SOURCE_MODE`, rejects invalid/contradictory combinations, requires stable promotion authorization, and requires a pinned rehearsal SHA unless the branch-rehearsal opt-in is present. The monitoring workflow exposes the mode and revision inputs and rejects stable mode without an explicit revision at [`deploy-05-operations-monitoring.yml`](.github/workflows/deploy-05-operations-monitoring.yml:71) and [`deploy-05-operations-monitoring.yml`](.github/workflows/deploy-05-operations-monitoring.yml:275).

2. **Rehearsal validation rejects an Application syncing stable — satisfied.**
   [`ch05-validate-operations-registration.sh`](platform/observability/validate/ch05-validate-operations-registration.sh:119) first bounds the live repo and then explicitly rejects the stable URL when the expected mode is rehearsal at [`ch05-validate-operations-registration.sh`](platform/observability/validate/ch05-validate-operations-registration.sh:122). The cited negative controls include this case and report it rejected.

3. **AppProject allowlisting is bounded without implicit switching — satisfied.**
   [`operations-project.yaml`](platform/observability/argocd/operations-project.yaml:22) contains only the two intended exact repository URLs. The registration script refuses a mode/URL mismatch before cluster access at [`ch05-register-operations-stack.sh`](platform/observability/scripts/ch05-register-operations-stack.sh:94), applies the bounded project, and asserts the live Application source at [`ch05-register-operations-stack.sh`](platform/observability/scripts/ch05-register-operations-stack.sh:141). The validator rejects wildcard and foreign project entries at [`ch05-validate-operations-registration.sh`](platform/observability/validate/ch05-validate-operations-registration.sh:138).

## Correctness and regression review

- The Application identity and project remain fixed; the registration path remains idempotent through `kubectl apply`, automated sync removal, and post-apply read-back assertions.
- The single-source guard is fail-closed for a non-empty `spec.sources`; absent/empty output is correctly treated as the expected single-source representation for an Application using `spec.source`.
- The monitoring workflow carries the resolved mode, repository, and revision into the remote environment. Its stable path intentionally fails closed unless the host-side promotion authorization exists; this prevents accidentally syncing the artifact SHA from the stable repository.
- Diagnostics default to the rehearsal mirror and do not expose a stable-mode selector. This is acceptable for the diagnostic-only path because it does not register or promote the Application; its repository/revision values remain explicit inputs and are passed to the diagnostic scripts.
- The changed scope is exactly the five authorized non-state paths. Controller-generated `tasks/**` modifications were not treated as product changes.
- No secret values or infrastructure commands were run during this review.

## Evidence reused

- `/tmp/platforminit-evidence/P-CH05-T02-static-checks.log`: `git diff --check`, both changed Bash files with `bash -n`, YAML parsing for both workflows and the AppProject, and the CH05 static reference-state gate all passed.
- `/tmp/platforminit-evidence/P-CH05-T02-negative-controls.log`: 22 controls, 0 failures; it covers stable/rehearsal resolution, missing and floating rehearsal revisions, stable authorization, implicit source switching, extra sources, wrong project, wildcard and foreign allowlist entries, and abbreviated stable revisions.
- No live-cluster registration/re-registration was performed. Consequently, the evidence proves repository logic and harness enforcement, not the state of a particular deployed cluster. A live read-only registration validator run remains release/runtime evidence for the Release Manager or operator to obtain.

## Consolidated defects

None found in the bounded changed scope. The implementation-reported legacy `PlatformInit` URL casing issue, manual rehearsal invocation requirement, coordinated stable promotion requirement, empty `spec.sources` behavior, and absent live-cluster evidence are documented operational risks rather than defects in the reviewed patch.
