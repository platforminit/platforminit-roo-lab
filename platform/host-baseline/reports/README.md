# Reports

Runtime reports belong here when using a persistent checkout.
Typical files:
- restore-smoke-<timestamp>.md
- validate-host-<timestamp>.md
- validate-host-<timestamp>.json
- drift-<timestamp>.md

For GitHub/release execution, reports should be written to the runtime root (for example `/srv/ch01-runtime/reports`), not committed back to the repository.
